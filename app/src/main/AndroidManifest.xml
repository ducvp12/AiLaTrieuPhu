<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vn.ailatrieuphu.ailatrieuphu.ducailatrieuphu">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PlayingActivity"></activity>

        <activity android:name=".loginfirebase.LoginActivity" />
        <activity android:name=".loginfirebase.LoginFirebaseActivity" />
        <activity
            android:name=".HomeActiveActivity"
            android:label="@string/title_activity_home_active"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".DangNhapActivity" />
        <activity android:name=".HomeActivity" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>